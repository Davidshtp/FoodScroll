# ============================================================
# FoodScroll - Backend Upstream Definitions
# Internal services are resolved via Docker DNS
# ============================================================

# No explicit upstream blocks needed since we use
# proxy_pass with DNS resolution.
# Docker Compose internal DNS resolves service names.
# Example: proxy_pass http://gateway:3000;
